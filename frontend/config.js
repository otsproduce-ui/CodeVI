/**
 * CodeVI - Global Configuration
 * Shared constants and configuration for all frontend scripts
 * Using var instead of const to allow redeclaration if needed
 */
var GRAPH_API_BASE = '/api/v1';
var API_BASE = '/api/v1';

